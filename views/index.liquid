<!DOCTYPE html>
<html lang="en">
    <head>
        {% include "partials/_analytics.liquid" %}
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {% assign title = "Sutta Basket" %}
        {% assign description = "Sutta Basket &mdash; unpacking Buddhist wisdom" %}
        {% assign url = "https://suttabasket.com" %}
        {% include "partials/_metatags_general.liquid" %}
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <header>
            <div id="header-text">
                <h1>Sutta Basket</h1>
                <h2>unpacking Buddhist wisdom</h2>
            </div>
            <nav>
                <a href="/tags">tags</a>
                |
                <a href="/about">about</a>
                |
                {% include "partials/_social.liquid" %}
            </nav>
        </header>
        <main>
            {% assign isHome = true %}
            {% for post in posts.all_posts %}
                {% include "partials/_preview.liquid" %}
            {% endfor %}
        </main>
        {% include "partials/_footer.liquid" %}

        <script src="/scripts/format-local-dates.js"></script>
    </body>
</html>