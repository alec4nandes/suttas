{% if post.tags.length > 0 %}
    <ul class="tags commas">
        tags:
        {% for tag in post.tags %}
            <li>
                <a href="/tags/{{ tag | slugify }}.html">{{ tag }}</a>
            </li>
        {% endfor %}
    </ul>
{% endif %}